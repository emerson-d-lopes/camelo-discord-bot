# Supply-chain hardening: refuse dependency versions published less than 3 days
# (4320 minutes) ago, so a freshly-compromised release can't be pulled into a
# build before it's caught and yanked. pnpm 10+ honours this.
minimum-release-age=4320
